# Dossier — Self-deploy setup
# Copy to .env.local and fill in. Or use the setup flow at /setup.

# Required: Anthropic API key for planning LLM and claude-flow builds
# Get from https://console.anthropic.com/ — never commit real keys
ANTHROPIC_API_KEY=your-anthropic-api-key-here

# Required: GitHub token for pushing branches and creating PRs in your project repo
# Create at: https://github.com/settings/tokens (repo scope)
GITHUB_TOKEN=ghp_...

# Optional: Database driver (default: sqlite)
# DB_DRIVER=sqlite

# Optional: For Postgres mode (hosted)
# DATABASE_URL=postgresql://...

# Optional: Data directory (default: ~/.dossier/)
# DOSSIER_DATA_DIR=~/.dossier/

# Optional: Embedding model (default: all-MiniLM-L6-v2, 384 dimensions)
# See ruvector-onnx-embeddings-wasm: all-MiniLM-L6-v2, bge-small-en-v1.5, e5-small-v2, etc.
# EMBEDDING_MODEL=all-MiniLM-L6-v2

# Supabase (if using hosted Postgres)
# NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
# NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Feature flags (set to false to disable)
NEXT_PUBLIC_PLANNING_LLM_ENABLED=true
NEXT_PUBLIC_BUILD_ORCHESTRATOR_ENABLED=true
NEXT_PUBLIC_MEMORY_PLANE_ENABLED=true

# Planning LLM
PLANNING_LLM_MODEL=claude-sonnet-4-5-20250929
NEXT_PUBLIC_DEFAULT_PROJECT_ID=
