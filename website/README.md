# Dossier marketing / download site

Static site and download landing for Dossier. Lives in this repo as a subdirectory so it can link to GitHub Releases and stay in sync with the product.

**From repo root:**

- `pnpm run website:install` — install dependencies (runs `npm install` in `website/`)
- `pnpm run website:dev` — run dev server
- `pnpm run website:build` — build for production

**From this directory:** `npm install`, `npm run dev`, `npm run build`.
